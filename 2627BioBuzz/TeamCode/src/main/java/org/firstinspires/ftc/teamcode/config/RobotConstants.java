package org.firstinspires.ftc.teamcode.config;

public class RobotConstants {
    // Test motor
    public static final double TEST_MOTOR_MAX_VELOCITY = 2500.0; // ticks per second
        // PIDF coefficients for the test motor
    public static final double TEST_MOTOR_P = 0.1;
    public static final double TEST_MOTOR_I = 0.0;
    public static final double TEST_MOTOR_D = 0.0;
    public static final double TEST_MOTOR_F = 0.0;

    // Controlls
    public static final double GAMEPAD_DEADZONE = 0.05; // Deadzone for gamepad sticks and triggers

    // Test loop
    public static final double TEST_LOOP_RUN_TIME = 2.0; // seconds
    public static final double TEST_LOOP_STOP_TIME = 2.0; // seconds
}